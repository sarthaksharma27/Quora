<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora - All Posts</title>
    <link rel="stylesheet" href="style.css"> 
</head>
<body>
  <img src=" " alt="" class="logo">
    <h1>Quora Posts</h1>
    <div class="post-container">
        <% for(post of posts) { %>
            <div class="post">
                <div class="user-info">
                    <h3 class="user">@<%= post.username %></h3>
                </div>
                <div class="post-content">
                    <p class="content"><%= post.content %></p>
                </div>
                <div class="post-actions">
                    <a href="http://localhost:3000/posts/<%=post.id%>" class="detail-link">See in detail</a>
                    <a href="http://localhost:3000/posts/<%=post.id%>/edit" class="edit-link">Edit</a>
                    <form method="post" action="/posts/<%= post.id %>?_method=DELETE" class="delete-form">
                        <button class="delete-button">Delete Post</button>
                    </form>
                </div>
            </div>
        <% } %>
    </div>

    <a href="http://localhost:3000/posts/new" class="create-post-link">Create New Post</a>
</body>
</html>
